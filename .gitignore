node_modules/
dist/
build/
**/.env
*.log
.svelte-kit/
.vite/
.wrangler/
.dev.vars
.DS_Store

# Local-only reference material (never committed — see ~/.claude/CLAUDE.md)
_ref/

# Claude Code session state — local-only, never committed
.claude/
